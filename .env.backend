# 仅后端部署配置
COMPOSE_PROFILES=backend

# 镜像配置
DOCKER_USERNAME=kalijerry
TAG=dev

# 端口配置
BACKEND_PORT=8080

# 网络配置
NETWORK_NAME=todolist-network
BACKEND_ALIAS=backend

# 后端配置
GIN_MODE=release
BACKEND_CONTAINER_NAME=todolist-backend